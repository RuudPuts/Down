machine:
  xcode:
    version: 8.3

deployment:
  staging:
    branch: develop
    commands:
      - bundle exec fastlane build
  beta:
    branch: develop
    commands:
      - bundle exec fastlane beta